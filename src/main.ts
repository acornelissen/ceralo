// SignetPDF frontend entry point.
// The walking skeleton renders nothing beyond the static shell yet; PDF
// rendering arrives in m0-4/m0-6. Kept intentionally tiny so the M0 build
// proves the toolchain, not features.

window.addEventListener("DOMContentLoaded", () => {
  // Mount point for the viewer, wired up in later milestones.
});
