{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults only. No plugin commands (dialog, fs, shell, http) are exposed to the frontend; the open dialog runs Rust-side inside the open_pdf command, so the JS surface is just the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
