{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus clipboard write-text only. The dialog runs Rust-side inside the open_pdf command (not exposed to JS); the only plugin command on the JS surface is clipboard-manager write-text, used by the context-menu Copy. Clipboard read is deliberately not granted.",
  "windows": ["main"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text"]
}
